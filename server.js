const express = require ("express");
      mongoose = require("mongoose");
      exphbs = require ("express-handlebars");
      bodyParser = require ("body-parser");
      cheerio = require("cheerio");
      axios = require("axios");


var db = require('./Scrapin-App/models')